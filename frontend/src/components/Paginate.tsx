import React from "react";

interface Props {
  pages: number;
  page: string;
  isAdmin: boolean;
  keyword: string;
}

const Paginate = ({ pages, page, isAdmin = false, keyword = "" }: Props) => {
  return;
};

export default Paginate;
